import React from "react";
function Greeting({ name }) {
  return <h1>Ghbdsn, {name}</h1>;
}
export default Greeting;

function Message({ text }) {
  return <p>{text}</p>;
}
export { Greeting, Message };

